/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['wattshare-images.s3.us-east-1.amazonaws.com'], // Add your image domain here
  },
};

module.exports = nextConfig; 